name: flu_ar_simple
description: AR Camera Simple
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  camera: ^0.10.5+5
  permission_handler: ^11.3.1
  path_provider: ^2.1.1
  path: ^1.8.3
  gal: ^2.3.0
  screenshot: ^3.0.0